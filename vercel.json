{
    "buildCommand": "npm run build",
    "installCommand": "pnpm install",
    "outputDirectory": "build",
    "rewrites": [
        {
            "source": "/(.*)",
            "destination": "/build/main.js"
        }
    ]
}