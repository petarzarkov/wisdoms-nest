{
    "installCommand": "pnpm install",
    "buildCommand": "npm run build",
    "framework": "other",
    "outputDirectory": "build",
    "rewrites": [
        {
            "source": "/(.*)",
            "destination": "/build/main.js"
        }
    ]
}